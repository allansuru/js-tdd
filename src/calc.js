


// eslint-disable-next-line no-undef


const sum = (a, b) => a + b;
const sub = (a, b) => a - b;
const mult = (a, b) => a * b;
const div = (a, b) => (b === 0) ? 'it`s not possible divide by zero' : a / b;
const outro = () => 'bla'
const outro2 = () => 'bla'
export {
    sum,
    sub,
    mult,
    div,
    outro,
    outro2
}